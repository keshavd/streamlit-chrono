import React from "react"
import ReactDOM from "react-dom"
import StreamlitChrono from "./StreamlitChrono";

ReactDOM.render(
  <React.StrictMode>
    <StreamlitChrono />
  </React.StrictMode>,
  document.getElementById("root")
)
